# Maintainer: Chupligin Sergey (NeoChapay) <neochapay@gmail.com>

pkgname=libwspcodec
pkgver=2.2.5
pkgrel=1
pkgdesc="Library of glib utilities"
arch=('x86_64' 'aarch64')
url="https://github.com/sailfishos/${pkgname}"
license=('LGPLv2')
depends=('glib2')
source=("${url}/archive/refs/tags/$pkgver.tar.gz")
sha256sums=('ffd2a34aa8940953f3600eed6108fdd2413f4f93928d068edbbc3a14efb44ab1')


build() {
    cd $pkgname-$pkgver
    make LIBDIR=/usr/lib KEEP_SYMBOLS=1 release pkgconfig
    make
}

package() {
    cd $pkgname-$pkgver
    make DESTDIR="$pkgdir/" LIBDIR=/usr/lib install-dev
}
